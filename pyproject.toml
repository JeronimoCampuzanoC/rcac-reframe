[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rcac-reframe"
version = "0.1.0"
description = "ReFrame orchestration for RCAC Negishi"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
rcac-reframe = "cli.main:main"
